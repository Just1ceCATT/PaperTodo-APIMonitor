{
  "kind": "native",
  "id": "api.balance.monitor",
  "name": "API 余额监测",
  "description": "通过 DeepSeek /user/balance 接口拉取余额，按余额提醒阈值显示不同颜色圆环。",
  "version": "1.2.0",
  "apiVersion": "2.0",
  "stateVersion": 2,
  "entry": "PaperTodo.Plugin.ApiBalanceMonitor.dll",
  "capabilities": [],
  "requires": [
    "backgroundUpdates"
  ],
  "advancedSettings": true,
  "primarySettings": 2,
  "settingCategories": [
    {
      "name": "供应商 API key"
    }
  ],
  "settings": [
    {
      "id": "pollSeconds",
      "type": "number",
      "name": "刷新间隔",
      "description": "胶囊折叠后仍会按此间隔轮询（已声明 backgroundUpdates）。",
      "min": 15,
      "max": 3600,
      "step": 15,
      "default": 60,
      "suffix": "秒",
      "quick": true
    },
    {
      "id": "disableRing",
      "type": "boolean",
      "name": "关闭圆环",
      "description": "勾选后胶囊不再显示圆环指示器，仅保留文字。MiniView 5h/周进度条不受影响。",
      "default": false,
      "quick": true
    },
    {
      "id": "deepseekApiKey",
      "type": "string",
      "name": "DeepSeek",
      "description": "⚠️ 本地明文存储。仅 DeepSeek 供应商使用；切换供应商时不会复用它。",
      "placeholder": "sk-…",
      "maxLength": 400,
      "default": "",
      "category": "供应商 API key"
    },
    {
      "id": "usageToken",
      "type": "string",
      "name": "DeepSeek 用量查询 Token",
      "description": "platform.deepseek.com 的用量查询 Token（非 API Key）。填写后监视面板显示近 7 天 Token 用量柱状图。留空则只监测余额。",
      "placeholder": "platform 用量 token",
      "maxLength": 400,
      "default": "",
      "category": "供应商 API key"
    },
    {
      "id": "minimaxApiKey",
      "type": "string",
      "name": "MiniMax",
      "description": "⚠️ 本地明文存储。仅 MiniMax 供应商使用；切换供应商时不会复用它。",
      "placeholder": "MiniMax key",
      "maxLength": 400,
      "default": "",
      "category": "供应商 API key"
    },
    {
      "id": "opencodeApiKey",
      "type": "string",
      "name": "OpenCode Go(开发中)",
      "description": "⚠️ 本地明文存储。仅 OpenCode Go 供应商使用；切换供应商时不会复用它；当前仅预留 Key 入口，监视面板返回「尚未适配该供应商」。",
      "placeholder": "opencode key",
      "maxLength": 400,
      "default": "",
      "category": "供应商 API key"
    },
    {
      "id": "zhipuApiKey",
      "type": "string",
      "name": "ZhiPu GLM(开发中)",
      "description": "⚠️ 本地明文存储。ZhiPu GLM 供应商 Key；当前仅预留 Key 入口，监视面板返回「尚未适配该供应商」。",
      "placeholder": "zhipu key",
      "maxLength": 400,
      "default": "",
      "category": "供应商 API key"
    },
    {
      "id": "mimoApiKey",
      "type": "string",
      "name": "MiMo(开发中)",
      "description": "⚠️ 本地明文存储。MiMo 供应商 Key；当前仅预留 Key 入口，监视面板返回「尚未适配该供应商」。",
      "placeholder": "mimo key",
      "maxLength": 400,
      "default": "",
      "category": "供应商 API key"
    },
    {
      "id": "codexApiKey",
      "type": "string",
      "name": "CodeX(开发中)",
      "description": "⚠️ 本地明文存储。CodeX 供应商 Key；当前仅预留 Key 入口，监视面板返回「尚未适配该供应商」。",
      "placeholder": "codex key",
      "maxLength": 400,
      "default": "",
      "category": "供应商 API key"
    }
  ]
}
