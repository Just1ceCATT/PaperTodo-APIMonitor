你是一名**资深 C# / WPF / 桌面插件架构师**。请对当前提供的插件代码进行结构化重构。

## 当前问题

该插件目前主要逻辑集中在一个约 **3000 行的单文件**中，存在以下问题：

* UI、业务逻辑、API 请求、数据模型、状态管理、定时任务、事件处理等职责混杂；
* 单个类/文件承担过多职责，违反单一职责原则；
* 方法之间存在较强耦合；
* 后续新增功能容易继续堆积到主文件；
* 代码难以定位、维护和测试；
* **必须保证现有功能和行为不发生变化。**

## 重构目标

将当前单文件拆分为职责明确、依赖清晰的模块，但**避免为了拆分而过度工程化**。

## 核心重构原则

### 1. 先分析，再修改

不要直接拆文件。

首先完整分析现有代码，并输出：

1. 当前主文件承担的所有职责；
2. 每个主要方法、字段、事件处理器分别属于哪个职责；
3. 现有依赖关系；
4. 推荐的模块划分；
5. 最终目录结构；
6. 每个文件的职责说明；
7. 重构风险点。

确认结构后，再开始重构。

### 2. 按“业务职责”拆分，而不是按代码行数机械拆分

例如：

* API 请求和认证逻辑 → `Services`
* DTO / API 响应对象 → `Models`
* UI 组件构建与更新 → `Views`
* 状态与 UI 绑定逻辑 → `ViewModels`
* 定时刷新与监控循环 → `Monitors` 或对应 `Service`
* 数据格式化 → `Helpers`
* 常量 → `Constants`
* 插件生命周期 → 保留在插件入口类

**禁止因为文件过长就随意拆成 `Part1`、`Part2`、`Utils`、`Common` 等无业务语义文件。**

### 3. 控制主入口职责

最终插件入口类应主要负责：

```text
插件生命周期
↓
初始化配置
↓
初始化 UI
↓
注册事件
↓
启动/停止各业务服务
```

不要继续在入口类中直接堆积：

```text
HTTP 请求
JSON 解析
复杂 UI 构建
定时循环
数据计算
业务判断
```

### 4. 降低耦合

拆分时重点处理：

* 直接跨模块访问字段；
* UI 与 API 调用混杂；
* 业务逻辑依赖具体 UI 控件；
* 定时任务直接修改大量 UI；
* 事件处理器包含完整业务流程；
* 多处重复的状态更新逻辑。

优先通过：

```text
明确的数据模型
↓
Service 返回业务数据
↓
ViewModel / Controller 协调状态
↓
View 更新 UI
```

进行组织。

### 5. 保持行为完全一致

这是最高优先级。

重构过程中：

* 不修改现有接口协议；
* 不修改请求参数；
* 不修改 API 地址；
* 不修改认证方式；
* 不修改刷新频率；
* 不修改 UI 功能；
* 不修改现有数据计算逻辑；
* 不擅自修复“可能存在的问题”；
* 不改变异常处理语义；
* 不改变线程模型，除非为了修复因拆分导致的明确错误。

本次任务首先是**结构重构，不是功能重写**。

### 6. 依赖方向必须清晰

尽量遵循：

```text
Plugin / View
    ↓
Coordinator / ViewModel
    ↓
Service
    ↓
API / Data
```

低层模块不要反向依赖高层 UI。

例如：

```csharp
// 不推荐
public class ApiService
{
    private readonly TextBlock _statusText;
}
```

应该改为：

```csharp
public class ApiService
{
    public Task<ApiResult> GetUsageAsync(...)
    {
        ...
    }
}
```

由上层决定如何展示结果。

### 7. 事件处理器必须轻量化

例如：

```csharp
private async void RefreshButton_Click(...)
{
    // 300 行业务逻辑
}
```

应尽量变为：

```csharp
private async void RefreshButton_Click(...)
{
    await _coordinator.RefreshAsync();
}
```

复杂流程进入对应的业务模块。

### 8. 避免过度 MVVM

如果当前项目是大量代码动态创建 WPF UI，而不是标准 XAML + MVVM 架构，**不要为了“形式正确”强行重写成完整 MVVM**。

应优先进行渐进式重构：

```text
现有 UI 构建逻辑
        ↓
独立 UI Builder / View
        ↓
业务 Service
        ↓
Model
```

只有在当前代码结构明确适合时，才引入完整 MVVM。

### 9. 拆分标准

一个模块通常应该满足：

* 职责单一；
* 可以通过名称明确知道用途；
* 不需要了解整个插件才能理解；
* 修改该模块不应频繁影响其他模块；
* 文件之间避免循环依赖。

但不要追求“每个文件只有几十行”。

**100～300 行的职责清晰文件通常是合理的。**

## 实际执行方式

请严格按照以下阶段执行。

### 阶段 1：代码审计

阅读完整代码后，输出：

```markdown
# 当前代码职责分析

## Plugin.cs
- 职责 A
- 职责 B
- 职责 C

## 主要问题
- ...
```

然后输出：

```markdown
# 推荐目录结构

PluginName/
├── ...
```

并说明每个文件：

```markdown
| 文件 | 职责 | 原代码来源 |
|---|---|---|
| ApiService.cs | API 请求 | Plugin.cs 的 xxx 方法 |
```

### 阶段 2：制定迁移计划

明确说明：

```text
Step 1：先提取 Models
Step 2：提取 API Service
Step 3：提取业务计算
Step 4：提取 UI 构建
Step 5：整理入口协调逻辑
Step 6：统一命名和依赖
Step 7：编译检查
```

每一步必须保证代码仍然可以独立编译。

### 阶段 3：开始重构

直接给出每个新增或修改文件的完整代码。

每次拆分必须：

1. 标明该文件路径；
2. 给出完整代码；
3. 说明从原文件迁移了哪些内容；
4. 同步更新依赖代码；
5. 保证命名空间正确；
6. 不保留失效的旧代码。

不要只给伪代码或代码片段。

### 阶段 4：最终检查

最终检查：

```text
[ ] 所有原有功能仍然存在
[ ] 没有重复实现同一逻辑
[ ] 没有循环依赖
[ ] API 调用逻辑已集中
[ ] UI 逻辑已与业务逻辑合理分离
[ ] 定时任务生命周期正确
[ ] CancellationToken / Timer 正确释放
[ ] HttpClient 生命周期合理
[ ] UI Dispatcher 调用位置正确
[ ] 没有后台线程直接操作 WPF UI
[ ] 插件 Stop / Dispose 后不会继续执行后台任务
[ ] 不存在未使用的字段、方法或 using
```

## 输出要求

如果代码过长，不要因为一次输出不下就降低质量。

优先采用以下方式：

```text
第一部分：完整架构分析
第二部分：完整目录结构
第三部分：迁移计划
第四部分：按文件逐个重构
```

不要：

* 只泛泛地给出架构建议；
* 只给目录结构不改代码；
* 只拆出几个文件就停止；
* 使用伪代码代替真实实现；
* 擅自修改现有功能；
* 为了“高级架构”引入大量不必要的抽象层。

最终目标是：

> **将当前 3000 行单文件重构为职责明确、结构清晰、可持续维护的插件工程，同时保持现有功能和运行行为一致。**

现在开始前，请先完整分析代码，不要立即修改。
